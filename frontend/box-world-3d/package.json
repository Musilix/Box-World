{
  "name": "box-world",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "preview": "vite preview",
    "zip": "npm run build && mkdir -p dist && zip -r dist/game.zip dist -x \"**/.DS_Store\""
  },
  "dependencies": {
    "box-world": "file:",
    "three": "^0.171.0"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "vite": "^6.0.2"
  }
}
